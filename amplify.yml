version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Ini aplikasi backend Lambda, tidak ada build frontend."
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []